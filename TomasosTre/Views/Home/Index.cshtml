@using Microsoft.AspNetCore.Mvc
@{
    ViewData["Title"] = "Home Page";
}
@model TomasosTre.ViewModels.Home.IndexViewModel

@* Search menu *@
<div class="col-md-12" id="menuSearch">
    @*<form action="@Url.Action("Search","Home")" method="get">
        <div class="input-group">
            <input name="phrase" type="text" class="form-control" placeholder="Search">
            <div class="input-group-btn">
                <button class="btn btn-default" type="submit">
                    <i class="glyphicon glyphicon-search"></i>
                </button>
            </div>
        </div>
        <div id="livesearch"></div>
    </form>*@
    <select class="js-example-data-array"></select>
</div>
<div class="col-md-12" id="cart">

</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            //var data = @Model.Dishes ;

            var data = [{ id: 0, text: 'enhancement' }, { id: 1, text: 'bug' }, { id: 2, text: 'duplicate' }, { id: 3, text: 'invalid' }, { id: 4, text: 'wontfix' }];
            $(".js-example-data-array").select2({
                placeholder: "Select a dish to order",
                data: data
            });
        });
    </script>    
}
